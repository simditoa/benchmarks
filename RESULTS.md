# Benchmark results

_No benchmark run has been recorded yet._

Trigger the **Run benchmark on GCP** workflow from the GitHub Actions tab to
populate this file. The workflow provisions a GCP C3 VM (Sapphire Rapids,
AVX-512 IFMA + VBMI; default `c3-standard-8`), runs the benchmark suite,
summarizes the output via the OpenAI API, and commits the result back to
this file.
